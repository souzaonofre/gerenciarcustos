{{ form_start(form) }}

  <div id="form-mov-widget" class="col-sm-6 pt-3">
    {{ form_widget(form) }}
  </div>

  <div id="form-mov-submit" class="col-sm-6 pt-3">
    <button class="btn">{{ button_label|default('Save') }}</button>
    <a href="{{ path('departamento_index') }}" class="btn btn-link">Retornar</a>
  </div>

{{ form_end(form) }}