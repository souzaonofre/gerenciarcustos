{% extends 'base.html.twig' %}

{% block title %}Movimentacao{% endblock %}

{% block body %}
    <h1>Movimentacao</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ movimentacao.id }}</td>
            </tr>
            <tr>
                <th>Data</th>
                <td>{{ movimentacao.data ? movimentacao.data|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Fornecedor</th>
                <td>{{ movimentacao.fornecedor }}</td>
            </tr>
            <tr>
                <th>Descricao</th>
                <td>{{ movimentacao.descricao }}</td>
            </tr>
            <tr>
                <th>Valor</th>
                <td>{{ movimentacao.valor }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('movimentacao_index') }}">back to list</a>

    <a href="{{ path('movimentacao_edit', {'id': movimentacao.id}) }}">edit</a>

    {{ include('movimentacao/_delete_form.html.twig') }}
{% endblock %}